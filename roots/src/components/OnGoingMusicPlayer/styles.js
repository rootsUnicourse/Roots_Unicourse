import { makeStyles } from '@material-ui/core'

export default makeStyles(() => ({
    stickToBottom: {
        width: '100%',
        position: 'fixed',
        bottom: 0,
        top: "auto",
        backgroundColor: "534340",
    },
}))